task generateReadme {
    doFirst {
        def readmeFile = new File("${project.rootDir}/README.md")
        def content = readmeFile.text
        content = content.replaceAll("'co\\.infinum:collar:.+?'", "'co.infinum:collar:${versions.collar}'")
        readmeFile.setText(content)
    }
}