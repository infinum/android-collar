ext {
    def major = 2
    def minor = 0
    def patch = 0

    buildConfig = [
            "minSdk"    : 23,
            "compileSdk": 35,
            "targetSdk" : 35,
    ]
    releaseConfig = [
            "group"      : "com.infinum.collar",
            "version"    : "$major.$minor.$patch",
            "versionCode": major * 100 * 100 + minor * 100 + patch
    ]
}