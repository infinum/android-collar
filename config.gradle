ext {
    buildConfig = [
            "minSdk"    : 19,
            "compileSdk": 30,
            "targetSdk" : 30,
            "buildTools": "30.0.3"
    ]
    releaseConfig = [
            "group"      : "com.infinum.collar",
            "version"    : "1.3.0",
            "versionCode": 1 * 100 * 100 + 3 * 100 + 0
    ]
}